pipeline {
    
    agent any

    parameters {
        string(name: "IMAGE", description: "URL for the ECR image.")
    }

    environment {
            BANK_IMAGE = "${params.IMAGE}"
            AWS_ACCOUNT_ID = credentials('AWS_ACCOUNT_ID')
            AWS_REGION = credentials('AWS_Region')
    }

    stages {

        stage("Process") {
            steps{
                dir('k8s/microservices'){  
                    sh "envsubst < bank.yaml"
                }
            }
        }

        stage("Configure"){
            steps{
                withAWS(credentials: 'AWS_Ricky', region: credentials('AWS_Region')){
                    sh "aws eks update-kubeconfig --name rl-aline"
                }
            }
        }

        stage("Update Deployment"){
            steps{
                dir('k8s/microservices'){
                    withAWS(credentials: 'AWS_Ricky', region: credentials('AWS_Region')){
                        sh "envsubst < bank.yaml | kubectl apply -f -"
                    }
                }
            }
        }


    }

}